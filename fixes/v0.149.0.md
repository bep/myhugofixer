## v0.149.0 Deprecations

### chromastyles --omitEmpty flag

- The `--omitEmpty` flag on the `gen chromastyles` command is deprecated. Empty classes are now always omitted; the flag is no longer needed.
