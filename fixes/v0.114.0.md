## v0.114.0 Deprecations

### CLI flags

- `--verbose` → Use `--logLevel info` instead.
- `--debug` → Use `--logLevel debug` instead.

### .Site.RSSLink

- `.Site.RSSLink` → Use the Output Format's Permalink method instead, e.g. `.OutputFormats.Get "RSS" | .Permalink`.
